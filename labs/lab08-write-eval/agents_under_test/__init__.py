"""The two agents Lab 8's eval is written against.

Both are provided, identical between the starter and solution editions —
this lab is about the eval, not the agent. `good_agent` is a vendored copy
of Lab 3's solution loop; `buggy_agent` is the same loop with one line
changed: a tool exception becomes a silent "" instead of "error: ...".
"""
