[project]
name = "worksheet-common"
version = "0.1.0"
description = "Shared offline harness for the Agentic Engineering Worksheet labs"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/worksheet_common"]
